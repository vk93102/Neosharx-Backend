# backend package — exists for backwards compatibility.
# The Django project was originally scaffolded as 'backend' and the
# settings directory was later renamed to NFA_Football/.
# All production code still uses 'backend.*' import paths; this package
# satisfies those references by delegating to NFA_Football/.
